<nav class="navbar-top navbar-dark">
  <div class="container">
  <div class="nav navbar-nav">
    <div class="nav-brand">
    <%=link_to ("The Four Quarters"), root_url%>
  </div>
  </div>
</div>
</nav>
<nav class="navbar navbar-dark">
    <div class="container">
        <button type="button" class="navbar-toggler hidden-lg-up"
          type="button"
          data-toggle="collapse"
          data-target="#nav-content">
           &#9776;
        </button>
        <div class="collapse navbar-toggleable-md" id="nav-content">
            <ul class="nav navbar-nav">
                <li class="nav-item">
                  <%= link_to 'NBA', tag_path("nba"), class: "nav-link #{yield(:nba_active)}" %>
                </li>
                <li class="nav-item">
                  <%= link_to 'Draft/College', tag_path("draft"), class: "nav-link #{yield(:draft_active)}" %>
                </li>
                <li class="nav-item">
                  <%= link_to 'NFL', tag_path("nfl"), class: "nav-link #{yield(:draft_active)}" %>
                </li>
                <li class="nav-item">
                  <%= link_to 'UFC', tag_path("movies"), class: "nav-link #{yield(:draft_active)}" %>
                </li>
                <li class="nav-item">
                  <%= link_to 'Movies/Entertainment', tag_path("movies"), class: "nav-link #{yield(:draft_active)}" %>
                </li>

                <% if author_signed_in? %>
                  <li class="nav-item dropdown author-nav">
                    <a class="nav-link dropdown-toggle <%= yield(:author) %>" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                      <%= image_tag(current_author.gravatar_image_url) %>
                      <%= current_author.display_name %>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                      <%= link_to 'My posts', authors_posts_path, class: "dropdown-item" %>
                      <%= link_to 'Account', authors_account_path, class: "dropdown-item" %>
                      <%= link_to 'Logout', destroy_author_session_path, method: :delete, class: "dropdown-item" %>
                    </div>
                  </li>
              <% end %>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
